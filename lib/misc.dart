import 'package:flutter/material.dart';
import 'cards.dart';

class MiscPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Misc Times")),
      body: const Center(
        child: Text("Will probably add some more locations"),
      ),
    );
  }
}
